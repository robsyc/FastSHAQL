# knows is a self-relationship: members know members.
# Nest as deep as the data allows — here two levels.
{
  member {
    displayName
    knows {
      displayName
      knows {
        displayName
      }
    }
  }
}
