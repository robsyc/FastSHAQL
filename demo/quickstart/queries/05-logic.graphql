# Composable logic: OR over scalar branches, NOT around a relationship.
{
  work(where: { OR: [{ pages: { gt: 400 } }, { price: { lt: 12 } }] }) {
    title
    pages
    price
  }
  member(where: { NOT: { homeBranch: { city: { eq: "Antwerp" } } } }) {
    displayName
    branchCity
  }
}
