query RelationshipSelectedAndFiltered {
  person(where: { employer: { locatedIn: { name: { eq: "Amsterdam" } } } }) {
    name
    employer {
      name
      locatedIn {
        name
      }
    }
  }
}
