SELECT ?iri ?iriLength
WHERE {
  ?iri a/<http://www.w3.org/2000/01/rdf-schema#subClassOf>* <http://example.org/Item> .
  BIND(STRLEN(STR(?iri)) AS ?iriLength)
  FILTER(?iriLength > "30"^^<http://www.w3.org/2001/XMLSchema#integer>)
}
